# dependencies
/node_modules
.pnpm-store

# next.js
/.next/
/out/
next-env.d.ts

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
pnpm-debug.log*

# env
.env
.env.local
.env*.local

# agent skill directories
.agent/

# runtime data stores (sqlite db, agentmemory state) — never commit
/data/

# typescript
*.tsbuildinfo

# Local Netlify folder
.netlify

# AI session transcripts / chat exports — may contain secrets, NEVER commit
opencode-session-*.md
*.session.md
*-chat-export.md
/session-*.md

# secret-bearing files (keys, certs, credential dumps)
*.key
*.p12
*.pfx
*credentials*.json
*secret*.json
.env.production.local
.env*.production.local
